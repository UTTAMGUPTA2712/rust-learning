# PNG Metadata Parser

Read a .png file as a byte stream and extract metadata (like width/height) by following the PNG specification.
This introduces Structs, Enums, and how Rust interacts with raw memory/bytes without needing "Unsafe" code.