# A Redis-lite Key-Value Store:

Build a networked KV store that supports basic commands (GET, SET, DELETE) over TCP.
You'll use the Tokio runtime to handle multiple concurrent connections.

[Read about RESP protocol here](https://redis.io/docs/latest/develop/reference/protocol-spec/)
