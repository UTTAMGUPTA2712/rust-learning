# Custom Grep Tool

Build a command-line tool that searches for strings in files using std::fs and std::env.
You will learn how Rust handles Strings (which are complex) and how to handle errors gracefully using the Result type.